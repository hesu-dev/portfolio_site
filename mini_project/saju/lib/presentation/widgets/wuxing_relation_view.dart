import 'package:flutter/material.dart';

class WuXingRelationView extends StatelessWidget {
  const WuXingRelationView({super.key});

  @override
  Widget build(BuildContext context) {
    // 이후 너의 WuXingRelation / BloodRelation을 Dart로 옮기면
    // 여기서 상생/상극, 육친 등을 시각화하면 됨.
    return const SizedBox.shrink();
  }
}
